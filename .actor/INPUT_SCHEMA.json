{
  "title": "Yahoo Auctions Japan Scraper Input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "searchKeywords": {
      "type": "array",
      "title": "Search keywords",
      "description": "List of keywords to search on Yahoo! Auctions Japan. Each keyword is searched independently and results are combined into a single dataset. Use this for multi-category resale research.",
      "editor": "stringList",
      "prefill": []
    },
    "searchKeyword": {
      "type": "string",
      "title": "Search keyword (single)",
      "description": "Single keyword to search for. Kept for backward compatibility; ignored if searchKeywords is provided.",
      "editor": "textfield",
      "prefill": ""
    },
    "maxItems": {
      "type": "integer",
      "title": "Max items",
      "description": "Maximum number of items to collect across all keywords.",
      "default": 100,
      "minimum": 1
    },
    "maxPages": {
      "type": "integer",
      "title": "Max pages",
      "description": "Maximum number of result pages to fetch per keyword.",
      "default": 5,
      "minimum": 1
    },
    "webhookUrl": {
      "type": "string",
      "title": "Webhook URL",
      "description": "Optional. POST a JSON summary to this URL when the run completes. Works with Slack, Discord, n8n or Zapier webhooks.",
      "editor": "textfield",
      "default": ""
    },
    "proxyConfiguration": {
      "type": "object",
      "title": "Proxy configuration",
      "description": "Optional Apify proxy configuration",
      "editor": "proxy"
    }
  },
  "required": []
}
